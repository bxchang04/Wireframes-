// no longer in user

var dnd = dragNdrop({
  // element to be dragged (single DOM element)
  element: document.getElementById('draggable1'),
  // dropElements and/or CSS Selector(s) (false / DOM element(s))
});

//hard coded way to get each component to be draggable
/*dragNdrop({
  // element to be dragged (DOM element)
  element: document.getElementById('draggable2'),
  // dropElements and/or CSS Selector(s)
});*/
